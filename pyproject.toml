[project]
name = "kakeibo-app"
version = "0.1.0"
description = "Streamlit + SQLite 家計簿アプリ"
readme = "README.md"
requires-python = ">=3.13.11"
dependencies = [
  "streamlit>=1.54.0,<2",
]

[tool.uv]
# 必要なら index-url など追加できます（通常は空でOK）